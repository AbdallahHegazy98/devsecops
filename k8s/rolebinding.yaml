apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding

metadata:
  name: readonly-binding
  namespace: devsecops

subjects:

- kind: ServiceAccount
  name: readonly-user
  namespace: devsecops

roleRef:
  kind: Role
  name: readonly-role
  apiGroup: rbac.authorization.k8s.io